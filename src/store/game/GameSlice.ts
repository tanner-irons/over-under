import { createSlice, PayloadAction } from "@reduxjs/toolkit";

// const bagSlice = createSlice({
//     name: "bags",
//     initialState,
//     reducers: {
//         addDiscToBag: (state, { payload }: PayloadAction<Disc>) => {
//             return [{ ...state[0], discs: [...state[0].discs, payload] }];
//         }
//     }
// });

// export const { addDiscToBag } = bagSlice.actions;
// export const bagReducer = bagSlice.reducer;